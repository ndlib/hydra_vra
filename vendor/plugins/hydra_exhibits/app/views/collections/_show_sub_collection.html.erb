<h1>Sub Collection Title</h1>

<p>
  Description
</p>

<% if !@subset.nil?%>
  <h3>Matched subset</h3>
<% else %>
  <h3>Did not match subset</h3>
<% end %>
<% if @descriptions.length > 0 %>
  <% @descriptions.each do |description|%>
    <h3><%= link_to "Essay: \"#{description.title}\"", essay_path(params[:collection_id], @essay.pid) %></h3>
    <p>
      <%= description.introduction %>
      <%= link_to "[Read the complete essay]", essay_path(params[:collection_id], @essay.pid) %>
    </p>
  <% end %>
<%- else -%>
  <h3><%= "Essay forthcoming..." %></h3>
<%- end -%>

<h3><%= link_to "Complete list of sources related to <em>#{""}</em> at Notre Dame", catalog_index_path(:params => {"f[type_facet][]" => ""}) %></h3>

<hr />

<h2>Featured sources</h2>

<dl class="item-list">
</dl>
